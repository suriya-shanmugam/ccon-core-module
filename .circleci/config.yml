version: 2.1

executors:
  docker-executor:
    docker:
      - image: cimg/node:16.16  # Node.js image (you can change the version as needed)
    working_directory: ~/repo

jobs:
  build:
    executor: docker-executor
    steps:
      - checkout  # Pulls the source code
      - run:
          name: Set up Docker
          command: |
            docker version 
      - run:
          name: Build Docker image
          command: |
            docker build -t suriyashanmugam/ccon-dev-build:latest .
      
      - run:
          name: Push Docker image
          command: |
            docker push suriyashanmugam/ccon-dev-build:latest

workflows:
  version: 2
  build_and_push:
    jobs:
      - build
